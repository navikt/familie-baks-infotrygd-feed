spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/familie-baks-infotrygd-feed
    password: test
    username: postgres
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql=false
    hibernate:
      ddl-auto: none

AZURE_APP_WELL_KNOWN_URL: https://login.microsoftonline.com/navq.onmicrosoft.com/v2.0/.well-known/openid-configuration
#AZURE_APP_CLIENT_SECRET: testsecret